export enum CollectionName {
    revenue = 'revenue',
    JOBS = 'jobs',
}

export enum Payments {
    creditCard = 'creditCard',
    debit = 'debit',
    pix = 'pix',
    currency = 'currency',
}

export enum RevenueType {
    service = 'service',
    sale = 'sale',
}