export enum CollectionName {
    revenue = 'revenue',
    JOBS = 'jobs',
    EMPLOYEES = 'employees',
    PAYMENT_METHOD = 'paymentMethod',
}

export enum RevenueType {
    service = 'service',
    sale = 'sale',
}